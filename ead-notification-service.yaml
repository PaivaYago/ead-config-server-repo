server:
  port: 8084
  servlet:
    context-path: "/ead-course/"

spring:
  application:
    name: ead-notification-service
  datasource:
    url: jdbc:postgresql://localhost:5434/ead-notification-v2
    username: postgres
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        jdbc:
          lob:
            non-contextual-creation: true
  output:
    ansi:
      enabled: ALWAYS
  rabbitmq:
    addresses: amqps://exoocvsi:Cse2TeWuiUcLuiohi6lGxRuokK82CLwJ@gerbil.rmq.cloudamqp.com/exoocvsi

management:
  endpoints:
    web:
      exposure:
        include:
          - refresh
          - health

logging:
  level:
    com.ead: TRACE
    root: INFO
    org.springframework.web: DEBUG
    org.hibernate: INFO
